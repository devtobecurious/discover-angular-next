import { PreLoaderStrategy } from './pre-loader-strategy';

describe('PreLoaderStrategy', () => {
  it('should create an instance', () => {
    expect(new PreLoaderStrategy()).toBeTruthy();
  });
});
