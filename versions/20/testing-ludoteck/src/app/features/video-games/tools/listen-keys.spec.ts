import { ListenKeys } from './listen-keys';

describe('ListenKeys', () => {
  it('should create an instance', () => {
    const directive = new ListenKeys();
    expect(directive).toBeTruthy();
  });
});
