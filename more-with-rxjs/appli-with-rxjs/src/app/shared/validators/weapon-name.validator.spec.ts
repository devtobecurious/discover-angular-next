import { WeaponName.Validator } from './weapon-name.validator';

describe('WeaponName.Validator', () => {
  it('should create an instance', () => {
    expect(new WeaponName.Validator()).toBeTruthy();
  });
});
