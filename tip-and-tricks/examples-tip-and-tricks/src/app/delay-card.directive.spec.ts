import { DelayCardDirective } from './delay-card.directive';

describe('DelayCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayCardDirective();
    expect(directive).toBeTruthy();
  });
});
